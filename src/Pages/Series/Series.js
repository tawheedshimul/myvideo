import React from 'react';
import BookSearch from './BookSearch';

const Series = () => {
    return (
        <div>
            <BookSearch></BookSearch>
        </div>
    );
};

export default Series;